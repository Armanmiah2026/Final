# Sensei Tunnel V2 Update

Version 1.0.2 / versionCode 3.

- Live VPN Gate free-server refresh on opening Free Server List (configurable).
- Fastest reachable free-server shortcut using TCP latency probes.
- Free servers are saved into the normal OpenVPN Server List and marked as public/free.
- Saved OpenVPN servers support Favorites, latency testing and direct connect from long-press actions.
- Last latency/status metadata is persisted per server.
- Auto reconnect/failover can be enabled or disabled from Settings.
- Existing payload, server, OVPN import, V2Ray/Xray and connection flows are preserved.
